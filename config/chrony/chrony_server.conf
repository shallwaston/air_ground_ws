# Example Chrony server config for the UGV machine.
# Replace the subnet below with the real field network before deployment.
pool pool.ntp.org iburst
allow 192.168.0.0/24
local stratum 10
makestep 1.0 3
rtcsync
